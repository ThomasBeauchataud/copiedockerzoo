{% extends 'base.html.twig' %}

{% block title %}CompteRendu index{% endblock %}

{% block body %}
    <h1>CompteRendu index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>Etat_animal</th>
                <th>Grammage</th>
                <th>Detail</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for compte_rendu in compte_rendus %}
            <tr>
                <td>{{ compte_rendu.id }}</td>
                <td>{{ compte_rendu.date ? compte_rendu.date|date('Y-m-d') : '' }}</td>
                <td>{{ compte_rendu.etatAnimal }}</td>
                <td>{{ compte_rendu.grammage }}</td>
                <td>{{ compte_rendu.detail }}</td>
                <td>
                    <a href="{{ path('app_compte_rendu_emp_show', {'id': compte_rendu.id}) }}">show</a>
                    <a href="{{ path('app_compte_rendu_emp_edit', {'id': compte_rendu.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_compte_rendu_emp_new') }}">Create new</a>
{% endblock %}
